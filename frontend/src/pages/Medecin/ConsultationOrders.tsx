import { useEffect, useMemo, useState } from "react";
import { createImagingRequest, createLabRequest, createPrescription, fetchAvailableMedications, type AvailableMedication } from "../../api/doctor";
import { fetchImagingCatalogue, type ImagingCatalogueItem } from "../../api/imaging";
import { fetchLaboratoryCatalogue, type LabCataloguePayload } from "../../api/laboratory";

type Ensure = () => Promise<string | null>;
type Notice = { error?: boolean; text: string } | null;
type Exam = { id: string; kind: "LAB" | "IMAGING"; name: string; price: string; priority: string; indication: string };
type Rx = { medicationId: string; dosage: string; quantity: string; route: string; frequency: string; durationDays: string; notes: string };
const input = "w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 outline-none focus:border-aulia-teal dark:border-slate-700 dark:bg-slate-900 dark:text-white";
const Field = ({ label, children }: { label: string; children: React.ReactNode }) => <label className="block text-sm"><span className="mb-1 block font-medium text-slate-700 dark:text-slate-200">{label}</span>{children}</label>;
const Alert = ({ value }: { value: Notice }) => value ? <p className={`rounded-lg p-3 text-sm ${value.error ? "bg-red-50 text-red-700 dark:bg-red-950/30 dark:text-red-200" : "bg-aulia-mist text-aulia-navy dark:bg-aulia-teal/15 dark:text-aulia-mist"}`}>{value.text}</p> : null;

export function ConsultationExamOrder({ ensureConsultation }: { ensureConsultation: Ensure }) {
  const [catalogue, setCatalogue] = useState<LabCataloguePayload | null>(null); const [imaging, setImaging] = useState<ImagingCatalogueItem[]>([]);
  const [kind, setKind] = useState<Exam["kind"]>("LAB"); const [itemId, setItemId] = useState(""); const [priority, setPriority] = useState("NORMAL"); const [indication, setIndication] = useState(""); const [items, setItems] = useState<Exam[]>([]); const [notice, setNotice] = useState<Notice>(null); const [saving, setSaving] = useState(false);
  useEffect(() => { void Promise.all([fetchLaboratoryCatalogue(), fetchImagingCatalogue()]).then(([labs, scans]) => { setCatalogue(labs); setImaging(scans); }).catch(() => setNotice({ error: true, text: "Le catalogue des examens est indisponible." })); }, []);
  const choices = useMemo(() => kind === "LAB" ? (catalogue?.tests || []).filter((x) => x.active).map((x) => ({ id: x.id, name: x.name, price: String(x.price) })) : imaging.filter((x) => x.active).map((x) => ({ id: x.id, name: x.name, price: String(x.price) })), [catalogue, imaging, kind]);
  useEffect(() => { if (!choices.some((x) => x.id === itemId)) setItemId(choices[0]?.id || ""); }, [choices, itemId]);
  const add = () => { const choice = choices.find((x) => x.id === itemId); if (!choice || items.some((x) => x.id === choice.id && x.kind === kind)) return; setItems((x) => [...x, { ...choice, kind, priority, indication }]); setIndication(""); };
  const change = (index: number, patch: Partial<Exam>) => setItems((current) => current.map((x, i) => i === index ? { ...x, ...patch } : x));
  const submit = async () => { if (!items.length || saving) return; setSaving(true); setNotice(null); try { const id = await ensureConsultation(); if (!id) throw new Error("Impossible d’ouvrir la consultation."); const labs = items.filter((x) => x.kind === "LAB"); const scans = items.filter((x) => x.kind === "IMAGING"); if (labs.length) await createLabRequest(id, { labTestIds: labs.map((x) => x.id), priority: labs.some((x) => x.priority === "URGENT") ? "URGENT" : "NORMAL", notes: labs.map((x) => `${x.name}: ${x.indication || "Indication à préciser"}`).join("\n") }); await Promise.all(scans.map((x) => createImagingRequest(id, { imagingCatalogueId: x.id, examName: x.name, bodyPart: "À préciser", urgency: x.priority === "URGENT" ? "URGENT" : "ROUTINE", contrastAgentUsed: false, notes: x.indication }))); setItems([]); setNotice({ text: "Toutes les demandes sélectionnées sont enregistrées et rattachées à la consultation." }); } catch (e) { setNotice({ error: true, text: e instanceof Error ? e.message : "Les demandes n’ont pas été enregistrées." }); } finally { setSaving(false); } };
  return <div className="space-y-4"><div className="grid gap-3 md:grid-cols-3"><Field label="Type"><select className={input} value={kind} onChange={(e) => setKind(e.target.value as Exam["kind"])}><option value="LAB">Laboratoire</option><option value="IMAGING">Imagerie</option></select></Field><Field label="Examen"><select className={input} value={itemId} onChange={(e) => setItemId(e.target.value)}>{choices.map((x) => <option key={x.id} value={x.id}>{x.name} · {x.price} CDF</option>)}</select></Field><Field label="Priorité"><select className={input} value={priority} onChange={(e) => setPriority(e.target.value)}><option value="NORMAL">Routine</option><option value="URGENT">Urgent</option></select></Field></div><Field label="Indication clinique"><textarea className={input} rows={2} value={indication} onChange={(e) => setIndication(e.target.value)} placeholder="Contexte et justification" /></Field><button type="button" className="rounded-lg border border-aulia-teal/30 px-4 py-2 text-sm font-bold text-aulia-teal" onClick={add}>Ajouter à la sélection</button>{items.length > 0 && <div className="overflow-x-auto rounded-xl border border-slate-200 dark:border-slate-800"><table className="min-w-[700px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-900"><tr><th className="p-3">Examen</th><th className="p-3">Priorité</th><th className="p-3">Indication</th><th className="p-3" /></tr></thead><tbody>{items.map((x, i) => <tr key={`${x.kind}-${x.id}`} className="border-t border-slate-100 dark:border-slate-800"><td className="p-3 font-semibold text-slate-800 dark:text-white">{x.name}<span className="block text-xs font-normal text-slate-500">{x.kind === "LAB" ? "Laboratoire" : "Imagerie"} · {x.price} CDF</span></td><td className="p-3"><select className={input} value={x.priority} onChange={(e) => change(i, { priority: e.target.value })}><option value="NORMAL">Routine</option><option value="URGENT">Urgent</option></select></td><td className="p-3"><input className={input} value={x.indication} onChange={(e) => change(i, { indication: e.target.value })}/></td><td className="p-3"><button type="button" className="font-semibold text-red-600" onClick={() => setItems((current) => current.filter((_, index) => index !== i))}>Retirer</button></td></tr>)}</tbody></table></div>}<Alert value={notice}/><button type="button" disabled={!items.length || saving} onClick={() => void submit()} className="rounded-lg bg-aulia-teal px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{saving ? "Enregistrement…" : `Enregistrer ${items.length} examen(s)`}</button></div>;
}

export function ConsultationPrescriptionOrder({ ensureConsultation }: { ensureConsultation: Ensure }) {
  const [medications, setMedications] = useState<AvailableMedication[]>([]); const [search, setSearch] = useState(""); const [draft, setDraft] = useState<Rx>({ medicationId: "", dosage: "", quantity: "1", route: "ORAL", frequency: "DAILY", durationDays: "7", notes: "" }); const [lines, setLines] = useState<Rx[]>([]); const [instruction, setInstruction] = useState(""); const [notice, setNotice] = useState<Notice>(null); const [saving, setSaving] = useState(false);
  useEffect(() => { void fetchAvailableMedications().then(setMedications).catch(() => setNotice({ error: true, text: "Le catalogue pharmaceutique est indisponible." })); }, []);
  const filtered = useMemo(() => medications.filter((x) => `${x.name} ${x.code} ${x.category?.name || ""} ${x.category?.section?.name || ""}`.toLocaleLowerCase("fr").includes(search.toLocaleLowerCase("fr"))).slice(0, 20), [medications, search]);
  useEffect(() => { if (!filtered.some((x) => x.id === draft.medicationId)) setDraft((x) => ({ ...x, medicationId: filtered[0]?.id || "" })); }, [filtered, draft.medicationId]);
  const changeDraft = (patch: Partial<Rx>) => setDraft((x) => ({ ...x, ...patch })); const update = (index: number, patch: Partial<Rx>) => setLines((current) => current.map((x, i) => i === index ? { ...x, ...patch } : x)); const name = (id: string) => medications.find((x) => x.id === id)?.name || "Médicament";
  const add = () => { if (!draft.medicationId || !draft.dosage.trim() || lines.some((x) => x.medicationId === draft.medicationId)) return; setLines((x) => [...x, draft]); setDraft((x) => ({ ...x, dosage: "", quantity: "1", durationDays: "7", notes: "" })); };
  const submit = async () => { if (!lines.length || saving) return; setSaving(true); setNotice(null); try { const id = await ensureConsultation(); if (!id) throw new Error("Impossible d’ouvrir la consultation."); await createPrescription(id, { instruction, lines: lines.map((x) => ({ medicationId: x.medicationId, dosage: x.dosage, quantity: Math.max(1, Number(x.quantity) || 1), route: x.route, frequency: x.frequency, durationDays: Math.max(1, Number(x.durationDays) || 1), notes: x.notes })) }); setLines([]); setInstruction(""); setNotice({ text: "L’ordonnance complète est enregistrée avec les médicaments et tarifs officiels." }); } catch (e) { setNotice({ error: true, text: e instanceof Error ? e.message : "L’ordonnance n’a pas été enregistrée." }); } finally { setSaving(false); } };
  return <div className="space-y-4"><div className="grid gap-3 md:grid-cols-2"><Field label="Rechercher un médicament"><input className={input} value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Nom, code, section ou catégorie…" /></Field><Field label="Médicament"><select className={input} value={draft.medicationId} onChange={(e) => changeDraft({ medicationId: e.target.value })}>{filtered.map((x) => <option key={x.id} value={x.id}>{x.name} · {x.category?.section?.name || "Sans section"} / {x.category?.name || "Sans catégorie"}</option>)}</select></Field><Field label="Dosage et unité"><input className={input} value={draft.dosage} onChange={(e) => changeDraft({ dosage: e.target.value })} placeholder="Ex. 500 mg" /></Field><Field label="Quantité totale"><input className={input} type="number" min="1" value={draft.quantity} onChange={(e) => changeDraft({ quantity: e.target.value })}/></Field><Field label="Voie"><select className={input} value={draft.route} onChange={(e) => changeDraft({ route: e.target.value })}><option value="ORAL">Orale</option><option value="INTRAVENOUS">Intraveineuse</option><option value="INTRAMUSCULAR">Intramusculaire</option><option value="SUBCUTANEOUS">Sous-cutanée</option><option value="TOPICAL">Cutanée</option><option value="INHALATION">Inhalation</option></select></Field><Field label="Fréquence"><select className={input} value={draft.frequency} onChange={(e) => changeDraft({ frequency: e.target.value })}><option value="ONCE">Une seule prise</option><option value="DAILY">Une fois par jour</option><option value="BID">Deux fois par jour</option><option value="TID">Trois fois par jour</option><option value="QID">Quatre fois par jour</option><option value="PRN">Si besoin</option></select></Field><Field label="Durée (jours)"><input className={input} type="number" min="1" value={draft.durationDays} onChange={(e) => changeDraft({ durationDays: e.target.value })}/></Field><Field label="Précautions"><input className={input} value={draft.notes} onChange={(e) => changeDraft({ notes: e.target.value })} placeholder="Après repas, contre-indication…" /></Field></div><button type="button" className="rounded-lg border border-aulia-teal/30 px-4 py-2 text-sm font-bold text-aulia-teal disabled:opacity-50" disabled={!draft.medicationId || !draft.dosage.trim()} onClick={add}>Ajouter à l’ordonnance</button>{lines.length > 0 && <div className="overflow-x-auto rounded-xl border border-slate-200 dark:border-slate-800"><table className="min-w-[900px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-900"><tr><th className="p-3">Médicament</th><th className="p-3">Dosage</th><th className="p-3">Qté</th><th className="p-3">Voie / fréquence</th><th className="p-3">Durée</th><th className="p-3">Précautions</th><th className="p-3" /></tr></thead><tbody>{lines.map((x, i) => <tr key={x.medicationId} className="border-t border-slate-100 dark:border-slate-800"><td className="p-3 font-semibold text-slate-800 dark:text-white">{name(x.medicationId)}</td><td className="p-3"><input className={input} value={x.dosage} onChange={(e) => update(i, { dosage: e.target.value })}/></td><td className="p-3"><input className={input} type="number" min="1" value={x.quantity} onChange={(e) => update(i, { quantity: e.target.value })}/></td><td className="p-3"><div className="grid gap-2"><select className={input} value={x.route} onChange={(e) => update(i, { route: e.target.value })}><option value="ORAL">Orale</option><option value="INTRAVENOUS">IV</option><option value="INTRAMUSCULAR">IM</option><option value="SUBCUTANEOUS">SC</option><option value="TOPICAL">Cutanée</option></select><select className={input} value={x.frequency} onChange={(e) => update(i, { frequency: e.target.value })}><option value="DAILY">1×/jour</option><option value="BID">2×/jour</option><option value="TID">3×/jour</option><option value="QID">4×/jour</option><option value="PRN">Si besoin</option></select></div></td><td className="p-3"><input className={input} type="number" min="1" value={x.durationDays} onChange={(e) => update(i, { durationDays: e.target.value })}/></td><td className="p-3"><input className={input} value={x.notes} onChange={(e) => update(i, { notes: e.target.value })}/></td><td className="p-3"><button type="button" className="font-semibold text-red-600" onClick={() => setLines((current) => current.filter((_, index) => index !== i))}>Retirer</button></td></tr>)}</tbody></table></div>}<Field label="Instructions générales de l’ordonnance"><textarea className={input} rows={2} value={instruction} onChange={(e) => setInstruction(e.target.value)} placeholder="Conseils généraux remis au patient"/></Field><Alert value={notice}/><button type="button" disabled={!lines.length || saving} onClick={() => void submit()} className="rounded-lg bg-aulia-teal px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{saving ? "Enregistrement…" : `Enregistrer l’ordonnance (${lines.length} médicament(s))`}</button></div>;
}
